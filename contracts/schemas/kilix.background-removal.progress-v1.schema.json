{
  "$comment": "Frozen G5a F108/F115 v1 wire contract, 2026-08-25; incompatible changes require a new schema identity.",
  "$id": "urn:kilix:schema:kilix.background-removal.progress:v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "job": {
      "$ref": "urn:kilix:schema:kilix.media-job.progress:v1"
    },
    "phase": {
      "enum": [
        "accepted",
        "commit",
        "decode",
        "infer",
        "load-model",
        "postprocess",
        "preprocess",
        "resolve-profile",
        "verify-output",
        "write-output"
      ]
    },
    "schema": {
      "const": "kilix.background-removal.progress/v1"
    }
  },
  "required": [
    "job",
    "phase",
    "schema"
  ],
  "type": "object"
}
